import React from 'react';
import render from 'react-dom';

const ValuePosition = () => {
  return(
    <div className="segment">
      <p>Value Position Components here</p>
    </div>
  );
};

export default ValuePosition;
