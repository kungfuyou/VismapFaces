import C from '../actions/constants';
import initialState from '../state/initialState';

const facesReducer = function(state=[], action) {
  return [];
};

const faceReducer = function(state={}, action) {
  return {};
};

export default facesReducer;
