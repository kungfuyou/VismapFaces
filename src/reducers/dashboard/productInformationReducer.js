import C from '../../actions/constants';
import initialState from '../../state/initialState';

const productInformation = function(state=initialState.productInformation, action) {
  return state;
};


export default productInformation;
